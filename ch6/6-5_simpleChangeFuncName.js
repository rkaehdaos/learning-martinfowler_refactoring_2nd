function circumference(radius) {
    return 2 * Math.PI * radius;
}

function mig_circum(radius) {
    return mig_circumference(radius);
}

function mig_circumference(radius) {
    return 2 * Math.PI * radius;
}

export {circumference, mig_circumference};